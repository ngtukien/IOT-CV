# Dataset tự thu bằng ESP32-CAM (GIAI ĐOẠN 20-22).
# Chia THEO SESSION, không random theo frame (GIAI ĐOẠN 21).

path: ../datasets/own_split
train: train/images
val: val/images
test: test/images

names:
  0: person
