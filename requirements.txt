# Core backend: MAVLink + web GCS + MQTT.
# Đủ để chạy GIAI ĐOẠN 5 -> 15 với SITL, không cần cài ML stack.
pymavlink>=2.4.42
fastapi>=0.115
uvicorn[standard]>=0.32
websockets>=13.0
paho-mqtt>=2.1
numpy>=1.26
