# ML / Computer Vision stack (GIAI ĐOẠN 16 trở đi).
# Tách riêng vì torch + ultralytics rất nặng; CI không cần cài phần này.
ultralytics>=8.4.155
# CHẶN <5 CÓ CHỦ ĐÍCH: opencv-python đã nhảy major 4.x -> 5.0.0.93, chưa ai kiểm tra API.
# Luật ignore trong dependabot.yml chỉ ngăn BOT ĐỀ XUẤT, không ngăn pip CÀI — thiếu cận
# trên ở đây thì `pip install -r` vẫn kéo về 5.0.0.93 (đã kiểm chứng bằng uv pip compile).
opencv-python>=4.14.0.94,<5
albumentations>=2.0.8
pandas>=3.0.6
matplotlib>=3.11.2
