"""Unit test cho backend.

Toàn bộ test ở đây phải chạy được KHÔNG cần SITL, không cần hardware, không cần
ML stack — CI chỉ chạy `uv sync --extra dev` (pyproject.toml gốc). Phụ thuộc ML
nằm ở dự án uv riêng `ml/pyproject.toml` và không bao giờ được CI cài.
"""
